<div class="product-slider">
    <?php
        echo $this->getLayout()
        ->createBlock('Magento\Cms\Block\Block')
        ->setBlockId('cake')
        ->toHtml();
    ?>
</div>
<div class="product-slider">
    <?php
        echo $this->getLayout()
        ->createBlock('Magento\Cms\Block\Block')
        ->setBlockId('platters')
        ->toHtml();
    ?>
</div>
<div class="product-slider">
    <?php
        echo $this->getLayout()
        ->createBlock('Magento\Cms\Block\Block')
        ->setBlockId('cafe')
        ->toHtml();
    ?>
</div>

<script type="text/javascript" xml="space">
    require([
        'jquery',
        'bxSlider'
    ], function($){
        $(document).ready(function() {
            var minSlides,
            width = $(window).width();

        if (width > 400) {
            minSlides = 3;
        } else {
            minSlides = 2
        }
        var minSlides = $('.bxslider.cake-slider').bxSlider( {
                controls: true,
                captions: true,
                slideWidth: 300,  
                minSlides:minSlides,
                maxSlides:2,
                moveSlides:2
            });
        });
    });
</script> 
<script type="text/javascript" xml="space">
    require([
        'jquery',
        'bxSlider'
    ], function($){
        // $(document).ready(function() {
            var slideWidth,
            width = $(window).width();

        if (width > 740) {
            slideWidth = 300;
        } else if (width < 740) {
            slideWidth = 500;
        }
        else if (width < 400) {
            slideWidth = 300;
        }
        var  slideWidth = $('.cafe-slider').bxSlider( {
                controls: true,
                captions: true,
                slideWidth:slideWidth
            });
        // });
    });
</script>